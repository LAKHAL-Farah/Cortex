[Unit]
Description=Cortex sandbox fake SSH auth log generator (SANDBOX ONLY)
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/ssh_auth_sim.sh
Restart=on-failure
RestartSec=3s

[Install]
WantedBy=multi-user.target
