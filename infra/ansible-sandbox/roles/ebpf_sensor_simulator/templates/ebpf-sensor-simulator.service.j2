[Unit]
Description=Cortex sandbox fake eBPF/Falco alert generator (SANDBOX ONLY)
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/ebpf_sensor_sim.sh
Restart=on-failure
RestartSec=3s

[Install]
WantedBy=multi-user.target
