global:
  scrape_interval: 20s
  evaluation_interval: 20s

scrape_configs:
  - job_name: 'node_exporter'
    file_sd_configs:
      - files:
          - /file_sd/nodes.json
        refresh_interval: 30s