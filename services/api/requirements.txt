fastapi==0.115.0
uvicorn[standard]==0.30.6
# Username/password auth (app/auth.py): bcrypt for password hashing, PyJWT
# for signing/verifying the session tokens issued by POST /api/v1/auth/login.
bcrypt==4.2.0
PyJWT==2.9.0
sqlalchemy==2.0.35
psycopg2-binary==2.9.9
alembic==1.13.2
pytest==8.3.3
httpx==0.27.2
ruff==0.6.9
ansible-core==2.17.6
requests==2.32.3
pandas==2.2.3
scipy==1.13.1
scikit-learn==1.5.2
joblib==1.4.2
openstacksdk==4.18.0
neo4j==5.28.4
qdrant-client==1.12.1
# Local embedding model for the knowledge RAG pipeline (adr-0004) -- pulls in
# torch and transformers as transitive deps; expect the image to grow by
# ~500MB-1GB and the first `pip install` to take a while.
sentence-transformers==3.3.1
# Grounded chat generation over the retrieved chunks above (adr-0005) --
# ChatNVIDIA calls the hosted NVIDIA NIM endpoint, no model weights baked
# into the image (unlike the embedding model).
langchain-nvidia-ai-endpoints==1.4.3
